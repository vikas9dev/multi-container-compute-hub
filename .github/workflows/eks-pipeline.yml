# Manual EKS Deployment Pipeline using Reusable Builder
name: EKS Deployment (Manual)
on:
  workflow_dispatch:

jobs:
  # 1. Call the shared build and push workflow
  build:
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit

  # 2. Deploy to EKS after build finishes
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: EKS Deployment
        run: |
          echo "Amazon EKS Deployment Configuration Not Ready Yet..."
          # Future: Add AWS Auth and kubectl for EKS here
