apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim # will use this name for PVC
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
